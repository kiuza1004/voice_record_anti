# 🎙️ Voice Life Log - 일상 자동 녹음 & 정리 어플리케이션 개발 보고서

## 📌 1. 프로젝트 개요
**Voice Life Log**는 하루 일상을 매 1시간 단위로 자동 녹음하고, 저장된 내용을 시간대별로 200자 이내로 정돈 요약하여 일자별 카테고리로 관리하는 네이티브 감성의 웹/하이브리드 애플리케이션입니다.  
과거 일상에 대해 자연어로 질문하면 특정 날짜와 시간대에 일어난 일을 검색해 주고, 텍스트 및 **음성 설명(TTS)**으로 답변합니다.

---

## ✨ 2. 주요 기능 및 구현 현황

### ① 1시간 단위 자동 녹음 (`HourlyAutoRecorder`)
- **연속 타이머 & 백그라운드 녹음**: 매 1시간(3600초) 단위로 자동으로 음성을 녹음하고 세션을 전환합니다. (테스트용 1분/5분/1시간 설정 지원)
- **프로그레스 인디케이터**: 남은 시간 카운트다운 및 실시간 프로그레스 바 제공.
- **실시간 음성 인식 텍스트(STT)**: 마이크 입력 시 실시간 말소리가 화면에 노출됩니다.

### ② 200자 이내 일자별/시간대별 요약 및 보관 (`STTSummarizer` & `LifeLogStorage`)
- **200자 이내 자동 요약 엔진**: 녹음된 대화 및 소리 텍스트를 200자 이내로 핵심만 압축 요약합니다.
- **IndexedDB 기반 로컬 저장소**: `일자(YYYY-MM-DD)` 및 `시간대(HH:MM ~ HH:MM)` 카테고리로 자동 분류 보관되며, 음성 오디오 데이터(Blob)도 함께 저장됩니다.

### ③ 일자별 카테고리 조회 메뉴 (`tab-history`)
- **일자별 아코디언 카드**: 날짜별로 그룹화하여 카테고리화된 시간대별 200자 요약을 조회합니다.
- **오디오 바로 들어보기**: 녹음된 오디오 파일을 웹 브라우저에서 즉시 재생 가능합니다.
- **날짜 필터 및 전체보기**: 특정 날짜를 선택하여 과거 기록을 신속히 조회할 수 있습니다.

### ④ 일상 검색 & 자연어 음성 설명 메뉴 (`LifeLogQAAssistant`)
- **자연어 검색 & Q&A**: *"어제 오후 3시에 무슨 일 있었지?"*, *"회의 내용 찾아줘"* 등 질의 시 200자 요약문 및 텍스트 데이터를 검색하여 사건을 찾아줍니다.
- **음성 설명 (TTS - Text to Speech)**: **[말로 설명 듣기]** 버튼을 통해 검색 결과를 음성(한국어 SpeechSynthesis)으로 읽어줍니다.

### ⑤ 앱 설정 및 데이터 관리 (`tab-settings`)
- **무료 정책 및 Gemini API Key(선택사항)**: 기본 0원 무료 로컬 요약기를 사용하며, 원할 경우 Gemini Free Tier API 키 등록 지원.
- **데이터 백업 및 삭제**: JSON 형태의 전체 기록 백업 다운로드 및 데이터 초기화 기능 제공.

---

## 🛠️ 3. 기술 스택 및 아키텍처

| 구성 요소 | 기술 스택 | 설명 |
| :--- | :--- | :--- |
| **Frontend UI** | HTML5, CSS3 (Vanilla) | 모바일 네이티브 iOS/Android 뷰어 & 다크 모드 레이아웃 |
| **STT (음성 인식)** | Web Speech API (`SpeechRecognition`) | 브라우저 내장 무료 음성-텍스트 변환 엔진 |
| **TTS (음성 설명)** | Web Speech API (`SpeechSynthesis`) | 답변 텍스트를 말로 설명해주는 무료 음성 합성 엔진 |
| **오디오 캡처** | MediaRecorder API | 사용자 마이크 스트림 캡처 및 WebM 오디오 데이터 생성 |
| **데이터베이스** | IndexedDB | 브라우저 내장 로컬 데이터베이스 (일자별/시간별 보관) |
| **요약 엔진** | Local Key-Sentence Extractor / Gemini Free Tier | 200자 이내 제한 핵심 문장 추출기 |

---

## 📁 4. 프로젝트 파일 구조

```
voice_record_anti/
├── index.html              # 모바일 네이티브 스타일 메인 HTML & 4대 탭 메뉴
├── css/
│   └── style.css           # 네이티브 모바일 뷰어 스타일, 카드 & 애니메이션 CSS
├── js/
│   ├── storage.js          # IndexedDB 기반 데이터 저장/일자별 카테고리화 관리자
│   ├── stt_summarizer.js   # STT 음성인식 및 200자 이내 요약 엔진
│   ├── recorder.js         # 1시간 단위 자동 연속 녹음 & 타이머 컨트롤러
│   ├── qa_assistant.js     # 자연어 검색 Q&A 및 TTS 음성 설명 모듈
│   └── app.js              # 메인 이벤트 바인딩 및 탭 전환 컨트롤러
└── DEVELOPMENT.md          # 현재 개발 사항 정리 문서 (본 파일)
```

---

## 💵 5. 비용 고지 정책 (유저 규칙 준수)

- **100% 무료 (비용 0원)**: 본 앱은 외부 유료 서버나 클라우드 비용이 발생하지 않는 **기기 내장 무료 Web Speech API** 및 **로컬 DB/요약 알고리즘**을 사용하여 구축되었습니다.
- **선택적 유료 요소 차단**: 클라우드 API 사용 시 발생할 수 있는 비용을 원천 차단하였으며, Gemini API 키 입력 시에도 Free Tier(무료 범위) 내에서만 작동하도록 설계되었습니다.

---

## 🚀 6. 실행 가이드

1. **로컬 개발 서버 실행**
   ```bash
   python -m http.server 8080
   ```
2. **브라우저 접속**: `http://localhost:8080`
3. **사용 흐름**:
   - 마이크 접근 권한 **[허용]**
   - **[자동 녹음 시작]** 클릭 시 1시간 단위로 자동 정리 & 일자별 카테고리 저장
   - **[일자별 기록]** 탭에서 날짜별 200자 요약 확인
   - **[검색 & 답변]** 탭에서 사건 질의 및 **[말로 설명 듣기]** 실행
